{
  "required": [
    "convex": "^1.x",
    "openai": "^4.x",
    "@convex-dev/auth": "^0.x"
  ],
  "recommended": [
    "lamejs": "^1.x (for audio encoding)",
    "recharts": "^2.x (for charts)",
    "react-beautiful-dnd": "^13.x (for drag-and-drop)",
    "date-fns": "^2.x (for date handling)"
  ]
}
